fastapi == 0.108.0
pydantic-settings == 2.1.0
uvicorn == 0.25.0
websockets == 12.0  # Required for WebSocket support
urllib3==2.6.3
psycopg2-binary==2.9.9 # PostgreSQL adapter
sqlalchemy==2.0.25
alembic==1.13.1
PyJWT==2.8.0
email-validator==2.1.0
requests==2.32.4 # For inter-service communication
httpx==0.27.0 # Async HTTP client for api-game calls
pytest==8.0.0 # Testing framework
sentry-sdk[fastapi]==1.45.1
boto3==1.35.0  # AWS S3 SDK for asset storage
cryptography==43.0.1  # RSA signing for CloudFront signed URLs
livekit-api==0.8.2  # LiveKit server SDK for AccessToken minting (sandbox/stream)
mistune==3.0.2  # Markdown AST parser used by scripts/parse_srd.py
beautifulsoup4==4.12.3  # HTML table parser used by scripts/parse_srd.py
click==8.4.2  # Admin management commands (admin.py) — already present transitively via uvicorn, pinned here because we import it directly